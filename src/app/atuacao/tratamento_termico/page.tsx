import { SectorPage, sectorMetadata } from '../sector-page';

export const metadata = sectorMetadata('tratamento_termico');

export default function Page() {
  return <SectorPage slug="tratamento_termico" />;
}
